apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: j-llm-ingress
  namespace: kube-system
  annotations:
    alb.ingress.kubernetes.io/load-balancer-name: j-llm-alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS":443}]'
    alb.ingress.kubernetes.io/inbound-cidrs: '188.91.152.191/32'
spec:
  ingressClassName: alb
  rules:
  - host: ollama.leazardlabs.site
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ollama-svc
            port:
              number: 11434
      - path: /metrics
        pathType: Prefix
        backend:
          service:
            name: ollama-exporter-svc
            port:
              number: 8088
  - host: grafana.leazardlabs.site
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kube-prometheus-stack-grafana
            port:
              number: 80
